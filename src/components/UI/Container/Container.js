import styled from 'styled-components';
import { space, typography, position, layout, color } from 'styled-system';

const Container = styled.div`
  position: relative;
  ${space}
  ${typography}
  ${position}
  ${layout}
  ${color}
`;

export default Container;
